import 'package:flutter/material.dart';

class Friend {
  String name, image, message, msgTime;
  Color color;

  Friend({
    this.name,
    this.image,
    this.message,
    this.msgTime,
    this.color,
  });
}
